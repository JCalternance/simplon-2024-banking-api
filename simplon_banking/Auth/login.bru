meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{host}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "iousco",
    "password": "password"
  }
}

vars:post-response {
  token: res.body.accessToken
}

tests {
  const data = res.body;
}
